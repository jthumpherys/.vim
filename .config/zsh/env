#!/usr/bin/zsh

export XDG_CONFIG_HOME=$HOME/.config

setopt extendedglob
for env_file in $XDG_CONFIG_HOME/^zsh/env; do
  source $env_file
done

typeset -U path
path=(. ~/.local/bin $path)

export HOSTNAME=$HOST
