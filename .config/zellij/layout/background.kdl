// Layout to create on login to have background applications running and to speed up
//   creation of subsequent sessions

layout {
    tab name="spotify" {
        pane command="ncspot"
    }
    tab name="system" split_direction="vertical" {
        pane command="htop"
        pane command="kmon"
    }
}
